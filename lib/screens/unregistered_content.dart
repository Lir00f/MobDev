import 'package:flutter/material.dart';

class UnregisteredContent extends StatelessWidget {
  const UnregisteredContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Контент для НЕ зарегистрированных в системе");
  }
}
